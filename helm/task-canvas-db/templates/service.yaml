apiVersion: v1
kind: Service
metadata:
  name: {{ include "task-canvas-db.fullname" . }}
  labels:
    {{- include "task-canvas-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "task-canvas-db.selectorLabels" . | nindent 4 }}
