apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "task-canvas-db.fullname" . }}-test-connection"
  labels:
    {{- include "task-canvas-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "task-canvas-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
