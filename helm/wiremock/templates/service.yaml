apiVersion: v1
kind: Service
metadata:
  name: wiremock-svc
  labels:
    app: wiremock-svc
spec:
  type: NodePort
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
