apiVersion: v1
kind: Service
metadata:
  name: wiremock-svc
  labels:
    app: wiremock-svc
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
